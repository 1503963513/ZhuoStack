{
  "name": "@myapp/shared-types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types for frontend and backend",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
